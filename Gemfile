source "https://rubygems.org"

# Matches the GitHub Pages-supported Jekyll dependency set used in production.
gem "github-pages", group: :jekyll_plugins

# Jekyll's development server depends on WEBrick on current Ruby releases.
gem "webrick", "~> 1.8"
