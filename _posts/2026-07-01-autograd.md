---
title: "自动求导"
date: 2026-07-01
layout: single
permalink: /deep-learning/
sidebar:
  nav: docs
toc: true
toc_sticky: true
categories:
  - 深度学习
mathjax: true
mermaid: true
---

$$
\begin{array}{ccc}
\displaystyle\frac{\partial y}{\partial \mathbf{x}}
&
=
&
\displaystyle\frac{\partial y}{\partial u}
\displaystyle\frac{\partial u}{\partial \mathbf{x}}
\\[0.8em]
(1,n) && (1,)\quad(1,n)
\end{array}
\qquad
\begin{array}{ccc}
\displaystyle\frac{\partial y}{\partial \mathbf{x}}
&
=
&
\displaystyle\frac{\partial y}{\partial \mathbf{u}}
\displaystyle\frac{\partial \mathbf{u}}{\partial \mathbf{x}}
\\[0.8em]
(1,n) && (1,k)\quad(k,n)
\end{array}
\qquad
\begin{array}{ccc}
\displaystyle\frac{\partial \mathbf{y}}{\partial \mathbf{x}}
&
=
&
\displaystyle\frac{\partial \mathbf{y}}{\partial \mathbf{u}}
\displaystyle\frac{\partial \mathbf{u}}{\partial \mathbf{x}}
\\[0.8em]
(m,n) && (m,k)\quad(k,n)
\end{array}
$$

<aside class="callout callout--note" role="note">
  <p class="callout__title">💡$<\mathbf{x},\mathbf{w}>$</p>
  <p>表示向量 $\mathbf{x}$ 与向量 $\mathbf{w}$ 的内积。</p>
  <p>
    $$
    \frac{\partial \langle \mathbf{x}, \mathbf{w} \rangle}{\partial \mathbf{w}}
    =
    \mathbf{x}^\top
    $$
  </p>
</aside>

<div class="image-row">
  <img src="/images/autograd_01.png">
  <img src="/images/autograd_02.png">
</div>

用链式法则逐层乘回来，并在分支处**累加**梯度（去除不需要的枝）。[^1]

## 自动微分

### 标量的反向传播

在我们计算 $y$ 关于 $\mathbf{x}$ 的梯度之前，需要一个地方来存储梯度。

```
x = torch.arange(4.0)
x.requires_grad_(True)  
# 等价于 x = torch.arange(4.0, requires_grad=True)
x.grad  # 默认值是 None
```

执行语句后 PyTorch 会构建[^2]计算图，$\mathbf{x}$ 参与的所有运算都会被记录。现在计算 $y=x*x$：

$\mathbf{x}$ 是一个长度为 4 的向量，计算 $\mathbf{x}$ 和 $\mathbf{x}$ 的点积，得到了我们赋值给 $y$ 的标量输出。 接下来，通过调用反向传播函数来自动计算 $y$ 关于 $\mathbf{x}$ 每个分量的梯度，并打印这些梯度。

```
y.sum().backward()  # 以 y 为起点，对计算图执行反向传播，并把梯度写入所有叶子节点的 .grad 中。
x.grad  # 函数对变量 x 的梯度（偏导数）
```

```
tensor([0., 2., 4., 6.])
```

<div class="mermaid">
flowchart LR
    start["∂y/∂y = 1"] --> y["y = x²"]
    y --> local["∂y/∂x = 2x"]
    local --> x["x"]
    x --> out["x.grad = 2x"]
</div>

<aside class="callout callout--question" role="note">
  <p class="callout__title">❔为什么 $y$ 必须是标量？</p>
  <p>若 <code>y = torch.tensor([1.0, 2.0])</code>，PyTorch 不知道你要求和还是求平均值。</p>
</aside>

### 分离计算

有时，我们希望将某些计算移动到记录的计算图之外。

我们分离`y`来返回一个新变量`u`，该变量与`y`具有相同的值， 但丢弃计算图中如何计算`y`的任何信息。 换句话说，梯度不会向后流经`u`到`x`。

```
x.grad.zero_()
y = x * x
u = y.detach()
z = u * x

z.sum().backward()
x.grad == u
```

```
tensor([True, True, True, True])
```

因此，上面的反向传播函数计算`z=u*x`关于`x`的偏导数，同时将`u`作为常数处理， 而不是`z=x*x*x`关于`x`的偏导数。

### Python 控制流的梯度计算

使用自动微分的一个好处是： 即使构建函数的计算图需要通过 Python 控制流（例如，条件、循环或任意函数调用），我们仍然可以计算得到的变量的梯度。 在下面的代码中，`while`循环的迭代次数和`if`语句的结果都取决于输入`a`的值。

```
def f(a):
    b = a * 2
    while b.norm() < 1000:
        b = b * 2
    if b.sum() > 0:
        c = b
    else:
        c = 100 * b
    return c
```

让我们计算梯度。

```
a = torch.randn(size=(), requires_grad=True)
d = f(a)
d.backward()
```

我们现在可以分析上面定义的`f`函数。 请注意，它在其输入`a`中是分段线性的。 换言之，对于任何`a`，存在某个常量标量`k`，使得`f(a)=k*a`，其中`k`的值取决于输入`a`，因此可以用`d/a`验证梯度是否正确。

```
a.grad == d / a
```

```
tensor(True)
```

[^1]: 反向传播（Backpropagation）：在计算函数值之后，从“输出端”往回传播梯度，把每一步的局部导数逐层累积起来，最终得到输入变量的导数。
[^2]: 计算图（Computational Graph）：把数学计算拆成“节点 + 有向边”的图结构，用来记录每一步运算，从而在反向传播时自动求导。
